import { D6WeaponRangePipe } from './d6-weapon-range.pipe';

describe('D6WeaponRangePipe', () => {
  it('create an instance', () => {
    const pipe = new D6WeaponRangePipe();
    expect(pipe).toBeTruthy();
  });
});
